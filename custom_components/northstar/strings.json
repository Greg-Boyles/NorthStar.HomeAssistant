{
  "config": {
    "step": {
      "user": {
        "title": "Connect to NorthStar API",
        "description": "Enter your NorthStar API URL and Polestar credentials.",
        "data": {
          "api_url": "API URL",
          "email": "Email",
          "password": "Password",
          "enable_streaming": "Enable streaming (near real-time updates)"
        },
        "data_description": {
          "enable_streaming": "Uses server-side streaming for faster updates (polls every 60s vs 15min). Optional - uses more API resources."
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid credentials",
      "cannot_connect": "Cannot connect to NorthStar API",
      "timeout": "Connection timed out",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This account is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Polestar NorthStar Api Options",
        "data": {
          "update_interval": "Update interval (seconds)",
          "enable_streaming": "Enable streaming (near real-time updates)"
        },
        "data_description": {
          "enable_streaming": "Uses server-side streaming for faster updates (polls every 60s). Optional - uses more API resources."
        }
      }
    }
  }
}
